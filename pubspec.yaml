name: e_ducator
description: "E-ducator — Sistem Pengurusan Akademik IKM Johor Bahru."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.6
  provider: ^6.1.2
  intl: ^0.19.0
  fl_chart: ^0.68.0
  pdf: ^3.12.0
  printing: ^5.14.3
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ikm_logo.png
